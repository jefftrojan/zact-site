import AboutContent from '../../components/about/AboutContent'

export const metadata = {
  title: 'About — Zact',
  description: 'Why we’re building Zact: an AI teammate that participates in meetings in real time and handles the follow‑through.'
}

export default function AboutPage() {
  return <AboutContent />
}

